[build]
# Default builds are portable (x86-64-v3 baseline).

[profile.release-native]
# Applied via CARGO_PROFILE_RELEASE_NATIVE_RUSTFLAGS in CI/dev if needed.

[target.'cfg(all(target_arch = "x86_64"))']
# Uncomment for local dev machines:
# rustflags = ["-C", "target-cpu=native"]
