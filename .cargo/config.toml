[build]
# Prefer host CPU features (AVX2/AVX-VNNI/FMA on modern x86). Portable
# CI/release artifacts should override with CARGO_BUILD_RUSTFLAGS or
# `--config 'build.rustflags=["-C","target-cpu=x86-64-v3"]'`.

[target.'cfg(all(target_arch = "x86_64"))']
rustflags = ["-C", "target-cpu=native"]
